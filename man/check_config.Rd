% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{check_config}
\alias{check_config}
\title{Validate and complete configuration settings used by estimation functions}
\usage{
check_config(config, method, n_time_steps = NULL)
}
\arguments{
\item{config}{An object of class \code{estimate_R_config}, as returned by
\code{\link[=make_config]{make_config()}}.}

\item{method}{One of "non_parametric_si", "parametric_si", "uncertain_si",
"si_from_data" or "si_from_sample" (see details).}

\item{n_time_steps}{Optional number of time steps in the supplied incidence
series. When provided, \code{t_start} and \code{t_end} are checked against this
length using the same rules as in \code{\link[=make_config]{make_config()}}.}
}
\description{
Internal helper used by \code{\link[=estimate_R]{estimate_R()}}, \code{\link[=estimate_R_agg]{estimate_R_agg()}}, and
\code{\link[=wallinga_teunis]{wallinga_teunis()}} to fill in the generic defaults that are documented for
\code{\link[=make_config]{make_config()}} and then validate the resulting configuration.
}
\details{
The generic defaults populated here are \code{mean_prior}, \code{std_prior},
\code{cv_posterior}, and \code{mcmc_control}. Their meaning and defaults are
documented in \code{\link[=make_config]{make_config()}}; the method-specific checks reuse the same
parameter descriptions as \code{\link[=estimate_R]{estimate_R()}}.
}
\keyword{internal}

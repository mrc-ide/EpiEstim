% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/si_distributions.R
\name{si_start_values}
\alias{si_start_values}
\title{Starting values for the estimation of the serial interval}
\usage{
si_start_values(si_data, dist)
}
\arguments{
\item{si_data}{The data on dates of symptoms of pairs of infector/infected
individuals, as described in \code{\link[=estimate_R]{estimate_R()}}.}

\item{dist}{The parametric distribution of the serial interval, as in
\code{si_parametric_distr} in \code{\link[=make_config]{make_config()}}, e.g. "gamma" or
"gamma_offset_1".}
}
\value{
A named list of starting values for the parameters of the
distribution, e.g. \code{shape} and \code{scale} for the Gamma distribution.
}
\description{
Computes starting values for the parameters of the serial interval
distribution, used to initialise the maximum likelihood estimation of the
serial interval in \code{\link[=estimate_R]{estimate_R()}} with method "si_from_data". The
starting values match the mean and standard deviation of naive serial
intervals computed from the midpoints of the intervals in \code{si_data}.
}
\examples{
data("MockRotavirus")
si_start_values(MockRotavirus$si_data, "gamma")
}
\seealso{
\code{\link[=estimate_R]{estimate_R()}}, \code{\link[=make_config]{make_config()}}
}

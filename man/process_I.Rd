% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_incidence.R
\name{process_I}
\alias{process_I}
\alias{process_I.default}
\alias{process_I.numeric}
\alias{process_I.integer}
\alias{process_I.data.frame}
\alias{process_I.incidence}
\alias{process_I.incidence2}
\alias{process_I_vector}
\title{Process incidence input into a standard format}
\usage{
process_I(incid)

\method{process_I}{default}(incid)

\method{process_I}{numeric}(incid)

\method{process_I}{integer}(incid)

\method{process_I}{data.frame}(incid)

\method{process_I}{incidence}(incid)

\method{process_I}{incidence2}(incid)

process_I_vector(incid)
}
\arguments{
\item{incid}{One of the following
\itemize{
\item A vector (or a dataframe with a single column) of non-negative integers
containing the incidence time series; these can be aggregated at any time
unit as specified by argument \code{dt}
\item A dataframe of non-negative integers with either i) \code{incid$I}
containing the total incidence, or ii) two columns, so that
\code{incid$local} contains the incidence of cases due to local transmission
and \code{incid$imported} contains the incidence of imported cases (with
\code{incid$local + incid$imported} the total incidence). If the dataframe
contains a column \code{incid$dates}, this is used for plotting.
\code{incid$dates} must contains only dates in a row.
\item An object of class \code{\link[incidence:incidence]{incidence::incidence()}}
}

Note that the cases from the first time step are always all assumed to be
imported cases.}
}
\value{
A \code{data.frame} with incidence counts formatted for internal use in
EpiEstim. The output always includes columns \code{local} and \code{imported}, and
may additionally contain \code{dates} (for \code{incidence2} inputs).
}
\description{
Standardises incidence data supplied to EpiEstim functions.
}
\details{
\code{process_I()} accepts incidence inputs in several formats and converts them
into a common \code{data.frame} representation with columns \code{local} and
\code{imported}:
\itemize{
\item A vector, or a \code{data.frame} with a single column, is interpreted as total
incidence.
\item A \code{data.frame} with a column named \code{I} is interpreted as total incidence.
\item A \code{data.frame} with columns \code{local} and \code{imported} is used directly.
\item Objects of class \code{\link[incidence:incidence]{incidence::incidence()}} and
\code{\link[incidence2:incidence]{incidence2::incidence()}} are converted to an incidence \code{data.frame} before
processing.
}

In all cases, the first time step is forced to have zero local cases:
any cases observed at the first time step are treated as imported and moved
to \code{imported[1]}.

Missing values in incidence columns are replaced with 0. A column named
\code{dates}, if present, is preserved and excluded from numeric checks.
Negative incidence values are not allowed.
}

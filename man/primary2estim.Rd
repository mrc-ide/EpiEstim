% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primary2estim.R
\name{primary2estim}
\alias{primary2estim}
\alias{primary2estim.default}
\alias{primary2estim.fitdist}
\alias{primary2estim.fitdistcens}
\alias{primary2estim.CmdStanMCMC}
\alias{primary2estim.data.frame}
\title{Link primarycensored and EpiEstim}
\usage{
primary2estim(x, ...)

\method{primary2estim}{default}(x, ...)

\method{primary2estim}{fitdist}(
  x,
  dist,
  n = 1000,
  shift = 0,
  si_discr_args = list(),
  seed = NULL,
  log_params = NULL,
  ...
)

\method{primary2estim}{fitdistcens}(
  x,
  dist,
  n = 1000,
  shift = 0,
  si_discr_args = list(),
  seed = NULL,
  log_params = NULL,
  ...
)

\method{primary2estim}{CmdStanMCMC}(
  x,
  dist,
  n = 1000,
  shift = 0,
  si_discr_args = list(),
  param_map = NULL,
  ...
)

\method{primary2estim}{data.frame}(x, dist, shift = 0, si_discr_args = list(), ...)
}
\arguments{
\item{x}{A fit from \code{\link[primarycensored:fitdistdoublecens]{primarycensored::fitdistdoublecens()}}, a fit from the
model returned by \code{\link[primarycensored:pcd_cmdstan_model]{primarycensored::pcd_cmdstan_model()}}, a fit from
fitdistrplus, or a data frame of parameter draws (see details).}

\item{...}{Not used.}

\item{dist}{The cumulative distribution function of the serial interval
minus \code{shift}, used for the fit, e.g. \code{\link[stats:pgamma]{stats::pgamma()}}. Any cumulative
distribution function of a non-negative delay can be used, with
\code{log_params} or \code{param_map} for distributions not supported by name in
\code{\link[=estimate_R]{estimate_R()}} with method "si_from_data".}

\item{n}{A positive integer giving the number of serial interval
distributions to draw. For a Stan fit with fewer posterior draws, all
draws are used.}

\item{shift}{A non-negative real giving the shift of the serial interval,
i.e. the fit is to the serial interval minus \code{shift}. Use 1 for a fit to
serial intervals shifted by one day.}

\item{si_discr_args}{A named list of additional arguments to \code{\link[=discr_si]{discr_si()}},
which can contain \code{L}, \code{D}, \code{dprimary} and \code{primary_args}. \code{dprimary} and
\code{primary_args} should match those used for the fit.}

\item{seed}{An integer used as the seed for the random number generator
when drawing from a \code{\link[primarycensored:fitdistdoublecens]{primarycensored::fitdistdoublecens()}} fit.}

\item{log_params}{For a maximum likelihood fit; the names of the parameters
that are positive, and so drawn on the log scale. Defaults to the positive
parameters of the distributions supported by name in \code{\link[=estimate_R]{estimate_R()}} with
method "si_from_data" (e.g. \code{shape} and \code{scale} for \code{\link[stats:pgamma]{stats::pgamma()}}),
and otherwise to the parameters with a positive estimate.}

\item{param_map}{For a Stan fit; a function taking a matrix of posterior
draws of the parameters of the primarycensored Stan model (with columns
\code{params[1]}, \code{params[2]}, ...) and returning a data frame of the
corresponding parameters of \code{dist}, with one column per parameter. Only
needed for distributions other than those supported by name in
\code{\link[=estimate_R]{estimate_R()}} with method "si_from_data", e.g.
\code{function(draws) data.frame(rate = draws[, 1])}.}
}
\value{
A list with two elements:
\itemize{
\item \code{si_sample}: a matrix where each column gives one distribution of the
serial interval to be explored, as used by \code{\link[=estimate_R]{estimate_R()}} with method
"si_from_sample".
\item \code{si_parametric_distr}: the name of the parametric distribution of the
serial interval, with "_offset_1" added when \code{shift} is 1.
}
}
\description{
\code{\link[=primary2estim]{primary2estim()}} transforms a serial interval distribution estimated with
\link[primarycensored:primarycensored-package]{primarycensored} into a sample
of discrete serial interval distributions in the right format for input into
\code{\link[=estimate_R]{estimate_R()}} with method "si_from_sample". This allows the serial interval
to be estimated once and reused.
}
\details{
Three inputs are supported:
\itemize{
\item A maximum likelihood fit from \code{\link[primarycensored:fitdistdoublecens]{primarycensored::fitdistdoublecens()}}. \code{n}
parameter sets are drawn from the asymptotic normal distribution of the
estimates, with positive parameters drawn on the log scale. This is the
approach used by \code{\link[=estimate_R]{estimate_R()}} with method "si_from_data".
\item A Bayesian fit from the model returned by
\code{\link[primarycensored:pcd_cmdstan_model]{primarycensored::pcd_cmdstan_model()}} (a \code{CmdStanMCMC} object). \code{n}
evenly spaced posterior draws of the delay parameters are used, so this
is the way to use a Bayesian fit of the serial interval. The draws are
read with the fit's own \code{draws()} method, which needs the posterior
package, installed alongside cmdstanr when the fit was made.
\item A data frame of parameter draws, with one row per draw and one column per
parameter of \code{dist}, named as the arguments of \code{dist} (e.g. \code{shape} and
\code{scale} for \code{\link[stats:pgamma]{stats::pgamma()}}).
}

Fits from \code{\link[fitdistrplus:fitdist]{fitdistrplus::fitdist()}} and \code{\link[fitdistrplus:fitdistcens]{fitdistrplus::fitdistcens()}} are
also accepted. For most epidemiological data, a fit with
\code{\link[primarycensored:fitdistdoublecens]{primarycensored::fitdistdoublecens()}} is preferred as it accounts for the
censoring of both the primary and secondary events, and for right
truncation. Only the parameter estimates and their covariance are used, and
their names are checked against the arguments of \code{dist}.

Distributions beyond those supported by name in \code{\link[=estimate_R]{estimate_R()}} with method
"si_from_data" can be used by giving \code{log_params} for a maximum likelihood
fit, or \code{param_map} for a Stan fit.

Each parameter set is discretised with \code{\link[=discr_si]{discr_si()}}. The support runs up to
the largest 0.999 quantile of the primary censored serial interval across
the sample, and each distribution is right truncated at the end of this
support. When \code{shift} is smaller than 1, the probability of a serial
interval of zero is set to zero by truncating below 1.
}
\examples{
## load data on rotavirus
data("MockRotavirus")
si_data <- MockRotavirus$si_data

## estimate the serial interval by maximum likelihood, accounting for
## double interval censoring
censdata <- data.frame(
  left = si_data$SL - si_data$EL,
  right = si_data$SR - si_data$EL,
  pwindow = si_data$ER - si_data$EL,
  D = Inf
)
fit <- primarycensored::fitdistdoublecens(
  censdata, distr = "gamma", start = list(shape = 2, scale = 1)
)

## turn this into a sample of serial interval distributions
si_sample <- primary2estim(fit, dist = stats::pgamma, n = 100,
                           seed = 1)$si_sample

## use estimate_R to estimate the reproduction number
## based on these estimates of the serial interval
R_si_from_sample <- estimate_R(
  MockRotavirus$incidence,
  method = "si_from_sample",
  si_sample = si_sample,
  config = make_config(list(n2 = 50))
)

plot(R_si_from_sample)
}
\references{
Abbott, S. et al. \link[primarycensored:primarycensored-package]{primarycensored}:
Primary Event Censored Distributions. \doi{10.5281/zenodo.13632839}
}
\seealso{
\code{\link[=estimate_R]{estimate_R()}}, \code{\link[=discr_si]{discr_si()}}
}
